name: Arduino SAM D

on:
  workflow_call:
    inputs:
      dummy:
        required: false
        type: boolean

jobs:
  call-workflow:
    uses: ./.github/workflows/RunTestSuite.yml
    with:
      platform: arduino:samd
