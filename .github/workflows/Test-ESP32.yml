name: ESP32

on:
  workflow_call:
    inputs:
      dummy:
        required: false
        type: boolean

jobs:
  call-workflow:
    uses: ./.github/workflows/RunTestSuite.yml
    with:
      platform: esp32:esp32
