name: ESP8266

on:
  workflow_call:
    inputs:
      dummy:
        required: false
        type: boolean

jobs:
  call-workflow:
    uses: ./.github/workflows/RunTestSuite.yml
    with:
      platform: esp8266:esp8266
